import 'package:flutter/material.dart';

class TownshipsTab extends StatelessWidget {
  const TownshipsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Townships Tab'),
    );
  }
}
